export enum Enumvisibility {
  PUBLIC = 'public',
  PRIVATE = 'private',
  FOLLOWERS = 'followers',
}

export enum TypeInteractions {
  LIKE = 'like',
  COMMENT = 'comment',
  SHARE = 'share',
}

export enum EnumRoles {
  ADMIN = 'admin',
  USER = 'user',
}
